import SearchBar from "../SearchBar";

const Header = () => {
  return (
    <header>
      <div>
        <h1>Pokédex</h1>
      </div>

      <SearchBar />
    </header>
  );
};

export default Header;
